# 更新日志

本项目的所有显著变更都会记录在此文件中。

维护约定（面向用户视角，不照抄 commit）：

- 每合并一个功能 / 修复，就在 `[未发布]` 区按分类补一条用户能读懂的描述；
- 发版时把 `[未发布]` 标题改成 `[版本号] - 日期`，删减润色后同步到 GitHub Release；
- 分类：`新增` / `优化` / `修复` / `部署与其他` / `升级说明`。

## [未发布]

### 新增

- **Peer 归属地开箱即用**：此前需手动下载 `GeoLite2-City.mmdb` 放入指定目录，缺失时「国家/地区」一直显示 `-`；现启动时自动从镜像下载数据库到数据目录（仅首次，约 60MB，下载在后台进行不阻塞服务），下载完成即生效；手动放置的文件仍优先使用

### 优化

- 多服务器列表的保存改为防抖提交，避免每敲一个字符就向服务端提交一次完整列表
- 种子详情「用户」列表中「国家/地区」列移到「地址」左侧，先看归属地再看 IP 更直观
- **移动端状态分组下拉**：点列表顶部「全部 (N)」标题即可弹出状态分组菜单，快速切换 全部 / 活跃 / 下载中 / 做种中 / 已完成 / 暂停 / 校验 / 错误；分组计数与侧边栏同一口径，在侧边栏隐藏的分组这里也不会出现
- 桌面表格视图横向滚动条挪到列表底部、状态栏正上方，并加大加粗：此前原生滚动条贴在列表最底边且大半被底部渐隐吞掉，几乎察觉不到，现支持直接拖拽、点击/按住轨道跳转（无横向溢出时自动隐藏）
- 移动端悬浮工具胶囊新增「设置」入口，「添加任务」图标换为幼苗（种下一颗新种子），更贴合 PT 场景
- 触屏设备的种子选择框改为圆形，与卡片的圆角设计语言统一（桌面表格视图不变）
- 全局动效对齐 iOS：所有颜色 / 透明度过渡统一走标准缓动曲线；移动端长按菜单增加弹出动画并从按压位置长出；开关滑块与顶栏圆形按钮的缩放改用带回弹的弹簧曲线
- **MCP 接入信息只在真能用时才给出**：宿主网关（实测 fnOS 1.2.0604）对应用路径 `/app/**` 未携带宿主登录态票据的请求一律返回 200 + 纯文本 `invalid token`，外部 AI 客户端本就连不上网关路径。此前 MCP 未启用、或「直连端口」留空时仍会展示地址与配置 JSON，照抄必然失败；现在未启用时不再展示接入信息，网关部署下启用后又缺「直连端口」时改为提示先填端口并配置接入令牌

### 修复

- **安全**：修复「切换服务器」会清空 MCP 接入令牌的问题——保存连接配置时漏写了 MCP 相关项，导致重启后 MCP 变成无需鉴权的端点、直连端口与「允许高危操作」被一并关闭
- **安全**：MCP 直连端口现在必须配合接入令牌才会启动（未配置令牌时只记日志并跳过该端口，界面仍可正常使用）；保存设置时后端会拒绝「端口非空 + 令牌为空」的组合；运行期把令牌清空后，该端口的所有请求一律拒绝；设置界面在未填令牌时不允许开启直连端口，端口开启时也不允许清空令牌（编辑中的令牌会与端口一并提交）
- **安全**：MCP「添加种子」现在与界面入口一致，仅接受 http(s) 链接与磁力链接，避免有人借 Transmission 的权限读取本机任意文件
- 修复删除「当前服务器」后仍连着已删除服务器的问题：现在会自动切换到剩余的可用服务器并同步连接配置，界面数据一并刷新
- 修复编辑服务器地址或名称后密码被静默清空、界面却仍显示「密码已保存」的问题
- 修复删除种子后确认框不会自动关闭的问题
- 修复批量添加时「文件勾选」可能套用到另一个种子的问题（拖入的文件中含有解析失败的项时触发）
- 修复批量添加链接失败后会重复添加的问题：后端改为逐条添加并返回失败明细，已成功的不再重试
- 修复切换服务器、删除服务器、或服务端推送删除种子后，旧的选中项仍留在选区、批量操作可能作用到错误种子的问题
- 修复「轮询间隔」设置对界面兜底轮询无效的问题：WebSocket 不可用的环境此前固定每 5 秒刷新一次，现在跟随设置
- 修复批量「开始」后列表不立即刷新的问题（此前要等下一个轮询周期，与其它批量操作不一致）
- 修复未按「队列位置」排序时表格拖拽排序看似无效、却仍发出一串队列调整请求的问题，现在会提示先按队列位置排序
- 修复设置面板连续快速切换两个开关时，前一次改动在界面上被覆盖的问题
- 修复后端建种任务的进度查询存在并发读写、极端情况下可能读到不一致数据的问题
- GeoIP 数据库自动下载补齐超时与体积上限，避免镜像异常时拖住后台任务
- 修复 fnOS 下「数据目录」等处不显示语义化路径的问题：`trim.file.convertPath` 实际网关返回数组格式 data，此前仅按文档的对象格式解析导致真机上始终被判定为不可用而回退原始路径；现兼容数组/对象两种响应格式，请求关联 ID 对齐十进制格式，错误日志带上开放平台返回码便于定位宿主侧故障
- 修复侧边栏「站点分布」在上方分组较多时列表被面板底部裁切、无法下滑看全部站点的问题（与其他分组同口径参与侧栏整体滚动）
- 侧边栏「活跃」分组改为只统计实际有上传或下载流量的种子；处于下载/做种状态但没有传输的种子不再计入（底部状态栏计数同步调整）
- 修复滚动条迟迟才出现的问题：此前全局 scrollbar-width/scrollbar-color 会让新版 Chrome/Edge 禁用自定义滚动条并回退到自动隐藏的系统悬浮滚动条，现恢复为常驻的细滚动条（Firefox 走标准细滚动条）
- 侧栏磁盘剩余空间的「可用容量 / 总容量」改为同一行显示，不再折成两行（桌面侧栏与移动端抽屉同步调整）
- 接口返回非预期内容时给出明确提示：宿主网关在登录态失效等情况下会短路返回 200 + 纯文本（如 `invalid token`），此前界面只表现为「列表空白 + 一句笼统的请求失败」，现在会提示「宿主网关登录态已失效，请重新登录后重开本应用」
- **修复筛选条件残留导致种子列表长期空白**：侧栏「错误分布」选中一项后，顶栏的筛选标签行里**没有**对应标签，条件一旦失效（种子的错误信息会随 Transmission 重启 / 系统升级等变化）列表就永远空白，而侧栏和状态栏仍显示种子总数，看起来像「种子全丢了」；现在错误分布筛选会显示在筛选标签行内可单独移除，空列表也会区分「暂无数据」与「没有匹配的种子」并直接给出「清除全部筛选」入口（此前这颗按钮只在选中种子时才出现，普通用户无从下手）

### 部署与其他

- 镜像同步发布到 Docker Hub（`jianhuayanyu/trpanel`，国内拉取更快），与 GHCR 内容完全一致；README 与 docker-compose 默认镜像相应更新

## [0.1.1] - 2026-09-06

### 新增

- **MCP 直连端口可配置**：设置页可填写 / 清空 AI 客户端直连端口（1-65535 校验），留空即关闭并回显主服务地址；修改需重启后端生效（监听器随进程启动绑定）
- **MCP 接入信息一键复制**：MCP 设置面板提供「复制连接地址」「复制配置 JSON」，中英文文案同步；复制失败（如内嵌预览未授予剪贴板权限）会明确提示，而不是静默失败
- **分组限速**：把同一站点 / 标签 / 名称下的种子视为一组，为整组分别设定下载与上传的总上限（0 = 该方向不限）。引擎定期把「上限 − 组内已单独限速种子占用」后的余量均分给组内运行中的种子，使整组合计速度不超过上限；你单独设置过的限速与「跟随全局限速」标记不会被覆盖，删除规则或关闭引擎时自动还原。
  - 入口：设置 → 自动化 → 分组限速；侧边栏站点 / 标签项右键 →「为此分组添加限速规则」（自动预填范围）
  - 支持手动「立即应用」，并回显接管 / 下发 / 还原统计
- **批量限速**：选中多个种子后，顶栏批量工具条新增限速按钮，可统一设置或解除所选种子的下载 / 上传限速与连接数上限
- **内置 MCP 服务**：通过 `/mcp` 端点以工具形式暴露种子管理能力，AI 客户端（Claude、Qoder 等）可直接查询、添加、暂停 / 启动种子；支持接入令牌鉴权与「允许删除」开关，设置页修改即时生效
- **MCP 通用透传工具** `transmission_api_request`：可调用 Transmission 官方 RPC 白名单方法（覆盖客户端库未封装的能力）；只读方法直接可用，写方法受「允许通过 MCP 执行高危操作」开关管控，关停会话与宿主机脚本类敏感配置一律屏蔽
- **MCP 专用直连端口**：飞牛部署下可开启独立 TCP 端口（`MCP_PORT`，fpk 默认 9094）只暴露 `/mcp`，外部 AI 客户端无需经过统一网关即可接入；设置页展示直连地址
- **在线演示**：GitHub Pages 提供在线预览（推送到 master 自动构建部署），数据在浏览器内模拟生成、可交互（启停 / 删除 / 添加 / 设置均生效），界面右上角显示「演示模式」徽标；`/` 为电脑版，`/mobile.html` 为手机壳样式的移动版预览（真机打开自动进入应用本体）

### 优化

- 侧栏分组更清晰：目录分布更名为「数据目录」、标签分布更名为「种子标签」，种子标签下新增「全部 / 无标签」入口，顶栏激活筛选条同步识别「无标签」
- 底部状态栏「活跃 / 暂停 / 校验」计数改用与侧栏过滤器同一口径，两处数字不再对不上
- WebSocket 全量推送只做一次序列化，降低大列表下的 CPU 与内存峰值
- 做种策略预览复用状态快照，减少每轮评估的深拷贝开销
- 表格视图支持横向滚动（列总宽可超出视口），滚动条不再被玻璃渐隐遮住
- 列表列「下载 / 上传」更名为「下载速度 / 上传速度」，避免与限速值混淆

### 修复

- 单种子限速弹窗文案纠正：最大连接数填 **0 表示不连接任何 Peer**（并非不限），输入最小值改为 1；留空表示不修改
- 移动端 WebView 布局修复

### 部署与其他

- 新增 Dockerfile 与 CI，发布时自动构建多架构镜像
- 开发脚本 `dev.ps1` / `dev.sh`：后端支持 air 热重载，前端增加 HMR 就绪探测，避免拿到不热更的旧实例
- 项目协议变更为 MIT

### 升级说明

- 状态文件自动兼容：新增 `speedPolicy*` 字段，旧 `tm-state.json` 无需任何改动
- 分组限速引擎默认关闭，升级后需在 设置 → 自动化 → 分组限速 中手动开启

## [v0.1.0] - 2026-08-31

### 新增

- Transmission 管理面板核心：种子列表 / 详情 / 筛选排序 / 批量操作 / 添加种子
- 做种策略引擎：按站点设定分享率 / 做种时长 / 上传量目标，达标后自动暂停或删除
- 背景壁纸与玻璃折射、顶栏模糊搜索、问题反馈入口
- 内置 Transmission 开发 mock（`cmd/trmock`）、应用更新提示、活跃分类与选中态强化
- 五档布局分区、访问令牌输入、子路径 PWA

### 优化

- 飞牛（fnOS）专属特性抽象为宿主平台层，同一份代码支持通用部署
- 玻璃浓度调节，设置-关于文字清晰度提升

### 修复

- 收紧鉴权、路径读取与配置写入的安全边界
- 网关模式下信任宿主认证，放宽同源校验

[未发布]: https://github.com/sushazhi/trpanel/compare/v0.1.1...HEAD
[0.1.1]: https://github.com/sushazhi/trpanel/releases/tag/v0.1.1
[v0.1.0]: https://github.com/sushazhi/trpanel/releases/tag/v0.1.0
